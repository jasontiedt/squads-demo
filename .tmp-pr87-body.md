Closes #87.

Backfills typed `effect` payloads onto Action and Tactic cards in the catalog and tightens the schema so the loose-union escape hatch from #83/#90 is gone.

## What changed

**Catalog (`packages/assets-meta/data/`):**
- `english.json` — all Action and Tactic cards now carry typed `effect` payloads from the locked DSL (verbs: `draw`, `damage`, `heal-capital`, `gain-temporary-resource`, `buff-unit-stat`)
- `byzantines.json` — same

**Schema (`packages/schema/src/cards.ts`):**
- `ActionCard.effect` tightened from `z.union([Effect, z.unknown()])` to strict `Effect`
- `TacticCard.effect` likewise
- TODO referencing #87 removed
- `cards.test.ts` updated

**Worker (`apps/worker/test/post-actions.test.ts`):**
- Re-enabled the action-card happy path that was `it.skip`'d in #85. Now dispatches `PlayAction` against `eng-levy-the-fyrd` (typed `draw n=2`) and asserts hand → discard + 2 cards drawn via the dispatcher.

## Tests
```
packages/schema       228 passed
packages/assets-meta   26 passed
packages/rules        252 passed | 21 skipped
apps/worker            53 passed   (was 52 + 1 skip)
apps/web               77 passed
```

Unblocks #89 (integration arc).
