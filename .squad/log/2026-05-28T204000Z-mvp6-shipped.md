# Session log — MVP-6 shipped

**Date:** 2026-05-28T20:40:00Z
**Requested by:** Jason T
**Coordinator:** Squad (via Copilot)

## Summary

MVP-6 is complete. Eight PRs merged across seven numbered slices (S1–S7), all green on main.

- **S1 #97 → PR #104 (`bad61eb`)** — Artoo — Capital tile + queries.ts foundation
- **S2 #98 → PR #105 (`f718121`)** — Wedge — Effect DSL: attach-keyword + class-wide passive verbs
- **S3 #99 → PR #106 (`3ac1cc3`)** — Artoo — PlayUpgrade + PlayTechnology + `effectiveStats`
- **S4 #100 → PR #107 (`294d37e`)** — Artoo — PlayEvent + ≤3-active cap + per-turn tick
- **S5 #101 → PR #108 (`42160c2`)** — Artoo — PlayReaction + trigger taxonomy + opponent-window state
- **S6 #102 → PR #109 (`1240087`)** — Lando — Card-play UI + Reaction window modal
- **S7-A #103 → PR #110 (`60fbca4`)** — Artoo — Admin seed endpoint
- **S7-B #103 → PR #111 (`b3d0714`)** — Cassian — Playwright two-browser reaction-arc e2e, **shipped as scaffolding** (`test.skip`)

## S7-B status

The reaction-arc spec is fully written (~365 lines covering create/join → admin-seed → phase advance → deploy → attack → reaction modal → capital HP delta) but cannot execute end-to-end today. Diagnosis (Cassian via Copilot, confirmed by code walk):

- `Board.tsx:410-450` renders deployed units correctly as `<g data-testid="unit-${u.id}">` — selectors are right.
- `player.resources` starts as `[]` in `initialState.ts:145`.
- The only in-game resource source is `BuildCamp`, which returns `not_implemented` in `applyAction.ts:304`.
- Therefore no deploy can land via the UI today, regardless of card cost.

Two follow-up issues filed against `jasontiedt/squads-demo` to unblock:

- **#112** — Implement `BuildCamp` effect handler (rules-engine, larger scope).
- **#113** — Extend `admin-seed` to optionally seed `resources` + pre-deployed `units` on either player record (test-only, cheaper, recommended first).

Either lands → un-skip the e2e.

## Pinned facts (corrections to the original blocker doc)

- Admin seed body fields are `{ deckOrder, opponentDeckOrder, hand, opponentHand }`, **NOT** `{ hostDeck, guestDeck }`.
- `apps/e2e/node_modules` must be junctioned during worktree setup (default 6-path list misses it).
- Click `[data-target-legal="true"]` cells, not the overlay rect (`pointer-events: none`).

## Next

MVP-7 scope proposal from Wedge — likely starts with one of #112 / #113 to unskip the reaction-arc e2e baseline.
