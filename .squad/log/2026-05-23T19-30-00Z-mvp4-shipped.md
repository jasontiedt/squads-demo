# Session log — MVP-4 shipped

**Timestamp:** 2026-05-23T19:30:00Z UTC
**Requested by:** Jason
**Coordinator:** Squad (v0.9.4)

## Summary

MVP-4 closed in one coordinated session. Seven PRs merged. One blocker (#78) discovered mid-flight by Cassian during arc-test work and fixed same session by Artoo, unblocking the capital-zero arc. Three silent-success agent recoveries handled by coordinator.

## PRs merged

| PR  | Issue | Title                                          | Agent   | Notes                       |
|-----|-------|------------------------------------------------|---------|-----------------------------|
| #74 | #69   | schema: capital tileId + siegeState RFC        | Wedge   | RFC pinned for MVP-5        |
| #75 | #70   | rules: Move action handler                     | Artoo   |                             |
| #76 | #68   | rules: Capital-HP win condition                | Artoo   |                             |
| #77 | #73   | tests: integration arc suite                   | Cassian | found #78 blocker           |
| #79 | #78   | rules: Attack-vs-Capital (lift stub)           | Artoo   | unblocked #77 capital-zero  |
| #80 | #71   | web: interactive board surface                 | Lando   | silent-success recovery     |
| #81 | #72   | web: HUD (turn / phase / capital HP)           | Lando   |                             |
| #82 | #73 (follow-up) | e2e: capital-zero arc + handoff baseline | Cassian |                       |

## Decisions merged

2 inbox files consolidated into `.squad/decisions.md`:

- Wedge (#69) — capital `tileId` + `siegeState` RFC, locked shape for MVP-5 migration.
- Cassian — note on the `Attack`-against-building stub gap (now closed by #79).

Also added an `MVP-4 shipped` ledger entry mirroring the MVP-3 close convention.

## Blocker found & fixed mid-flight

`#78 — Attack against building is not_implemented`. Cassian's #77 arc tests could not complete the capital-zero arc through real `applyAction` calls because `attack.ts` rejected any `Attack` with `targetBuildingId`. Cassian flagged with `it.skip` + filed #78. Artoo lifted the stub in PR #79 same session, decrementing `Player.capitalHp` by `attacker.card[mode]`, exhausting the attacker, gated by phase / seat / Chebyshev range. Cassian re-enabled the skipped arc in #79 → #77 went green.

## Silent-success recoveries (3 this session)

- **Lando on #71 (interactive board).** Coordinator found 14 staged files, ran `pnpm -r test` (green), committed + pushed + PR.
- **Agent on #78.** Same shape — tests on filesystem, no text response. Finished from CWD.
- **Cassian on initial #72 attempt.** Empty first response; re-spawned and completed cleanly.

Pattern is stable. User memory note (`squad-coordinator-lessons.md`) holds.

## Worktree state

`squads-demo-71` directory orphaned on disk due to a Windows file lock (node_modules junction). Branch and worktree metadata cleanly removed from git. Harmless — will clean on next reboot.

## Carry-forward

- **MVP-5 capital-RFC migration** — apply the locked shape from PR #74 (single additive PR per Wedge's migration sketch).
- **PlayCard UI (#37)** — worker `?seat=X` unredact contract change still pending.
- **Capital init RFC closeout** — once MVP-5 migration lands, the `@needs-rfc` flag on `initialState` clears.

## HEAD

`main` advanced through #74 → #75 → #76 → #77 → #79 → #80 → #81 → #82 in roughly that order.
