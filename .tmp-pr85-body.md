Closes #85.

Recovery PR — Artoo's S3 dispatch was a silent-success across two attempts; coordinator landed the build fixes.

## What changed

**Schema (`packages/schema`):**
- `TemporaryBuff` schema on `UnitInstance.temporaryBuffs?: TemporaryBuff[]` (`expires: 'end-of-turn'`)
- `PlayCardAction` removed; `PlayActionCardAction` (`type: 'PlayAction'`) is the typed action-card play
- `Action` discriminated union + `ACTION_TYPES` tuple updated

**Rules (`packages/rules`):**
- `effects/dispatch.ts` — effect dispatcher routing on the verb of the locked Effect DSL (#83/#90)
- `playAction.ts` — typed Action-card handler (pay cost via #84/#91, hand → discard, dispatch effect, atomic rollback on error)
- Generic `playCard.ts` + its tests **deleted** (Wedge's call per `.squad/decisions.md`)
- `applyAction.ts` routes `PlayAction` → `playAction`; `PlayCard` case + phase legality removed
- `result.ts` — `effect_not_typed` error variant for cards whose `effect` payload doesn't match the locked union

**Web (`apps/web`):**
- `Lobby.tsx` non-unit-card click now dispatches `PlayAction` (was `PlayCard`)
- `PlayCardUi.needs-confirmation.test.tsx` expects `PlayAction`

**Worker (`apps/worker`):**
- Old PlayCard happy-path test marked `it.skip` with a TODO pointing at #87 (re-enable once Sabine's catalog backfill gives Action cards typed effects)

## Scope cuts (deliberate, locked by Wedge)

- **No card UI in MVP-5** — Lobby's action-card click works, but UIs for tactic/event/upgrade/technology stay deferred to MVP-6
- **Reactions deferred** to MVP-6
- **`target: 'units-by-class'`** is the Target taxonomy default; cross-seat ownership semantics use the locked `ownership: 'own' | 'opponent' | 'all'` per the Effect DSL

## Tests

```
packages/schema       228 passed
packages/assets-meta   26 passed
packages/rules        252 passed | 21 skipped
apps/worker            52 passed |  1 skipped (PlayCard → re-enable in #87)
apps/web               77 passed
```

Unblocks #86 (PlayTactic + EndTurn cleanup) and #89 (integration arc).
